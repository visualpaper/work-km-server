components:
  schemas:
    UsersResponse:
      type: object
      required:
        - users
      properties:
        users:
          type: array
          items:
            $ref: "./../definitions.yaml#/components/schemas/UserSchema"
